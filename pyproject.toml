[project]
name = "fish-kernel"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "UENO, M.", email = "eunosium-1128@live.jp" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/fish_kernel"]
